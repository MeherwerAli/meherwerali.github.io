# CV project bullets — release-gated draft

Do not copy these bullets into the private CV source until the corresponding public repository, `v0.1.0` release, and evidence report exist. The current numbers are local validation results dated 2026-08-15, not employer metrics.

- Built an OpenAI-compatible FastAPI reliability gateway with projected-cost reservation, atomic rate-limit contracts, bounded structured-output repair, signed webhook delivery, and hash-only policy audit events; validated 20 core/integration tests with 81% full-suite statement coverage across in-memory, PostgreSQL, and Redis paths locally.
- Built a page-grounded PDF research workbench with Qdrant dense/sparse fusion, claim-level citations, persisted approval expiry, resumable SSE, and a responsive TypeScript interface; validated 13 backend/integration tests, two UI state tests, and production container builds locally.
- Versioned an exactly 50-case reliability evaluation suite across retrieval, citations/abstention, structured output, and policy/approval behavior; the deterministic reference run passed 50/50 cases and all seven acceptance gates locally.

GPU latency, throughput, and memory bullets are intentionally absent until the approved NVIDIA L4 session publishes raw results.
